# Cloud Agents

This directory contains configuration and definitions for Cursor cloud agents.

## Structure

- `agent_config.yaml` - Main agent configuration
- `prompts/` - System prompts and templates
- `capabilities/` - Agent capability definitions

## Usage

Agents are configured via YAML files and can be deployed through the DevOps Brain workflow system.
